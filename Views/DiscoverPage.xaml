<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:FlickrApp.ViewModels"
             xmlns:shared="clr-namespace:FlickrApp.Views.Shared"
             x:Class="FlickrApp.Views.DiscoverPage"
             Title="Discover">
    <Grid RowDefinitions="Auto, Auto, *" Margin="10">
        <HorizontalStackLayout Grid.Row="0" Margin="0,10,0,10" Spacing="10">
            <Button Text="Top" WidthRequest="50" HeightRequest="30" CornerRadius="15" FontSize="12"/>
        </HorizontalStackLayout>


        <Label
            Grid.Row="1"
            Text="Popular:" TextColor="Black" FontSize="Medium" VerticalTextAlignment="Center"
            HorizontalTextAlignment="Start"
            Margin="10,0,0,10" />
        <shared:PhotoListView
            Grid.Row="2"
            ItemsSource="{Binding Photos}"
            LoadMoreItemsCommand="{Binding LoadMoreItemsCommand}"
            ItemTappedCommand="{Binding GoToPhotoDetailsCommand}" />

    </Grid>
</ContentPage>